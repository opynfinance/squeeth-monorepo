import { getMarkdown } from '@utils/markdown'
import MarkdownPage from '@components/MarkdownPage'

const BlockedPage = (props: any) => {
  return (
    <div>
      Seems you are using an VPN service, which is a violation of our terms of service.
    </div>
  )
}

export default BlockedPage
