declare module "@material-ui/core/styles/createPalette" {
  interface TypeBackground {
    stone: string,
    lightStone: string,
    tooltip: string,
  }
}

export enum Networks {
  MAINNET = 1,
  ROPSTEN = 3,
}
